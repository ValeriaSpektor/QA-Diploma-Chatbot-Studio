name: CI-CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Install Playwright Browsers
        run: npx playwright install

      - name: Install Allure CLI
        run: npm install -g allure-commandline --save-dev

      - name: Install Allurectl
        run: curl -sL https://github.com/allure-framework/allurectl/releases/download/v1.13.0/allurectl-linux -o allurectl && chmod +x allurectl && sudo mv allurectl /usr/local/bin/

      - name: Run Tests
        run: npx playwright test --reporter=allure-playwright

      - name: Generate Allure Report
        run: |
          allure generate allure-results --clean -o allure-report

      - name: Deploy Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GH_PAT }}
          publish_dir: ./allure-report

      - name: Upload results to Allure TestOps
        run: |
          allurectl upload --results-dir=allure-results \
            --project-id=4505 \
            --execution-name="Run ${{ github.run_number }}" \
            --token=${{ secrets.ALLURE_TOKEN }}

      - name: Send notification to Telegram
        if: ${{ always() }}
        run: |
          passed=$(grep -c '"status":"passed"' allure-results/* || true)
          failed=$(grep -c '"status":"failed"' allure-results/* || true)
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d '{
              "chat_id": "${{ secrets.TELEGRAM_CHAT_ID }}",
              "text": "Статус тестов: ${{ job.status }}.\n\nПрошло: '${passed}'\nПровалилось: '${failed}'\n\nПосмотреть отчет: https://<ваш_репозиторий>/actions"
            }'
